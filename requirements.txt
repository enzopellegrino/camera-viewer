# Su Linux usa PySide6-Essentials (più leggero, ~500MB invece di ~900MB)
# Su Mac/Windows usa PySide6 completo per lo sviluppo
PySide6-Essentials>=6.6.0; sys_platform == "linux"
PySide6>=6.6.0; sys_platform != "linux"
pyinstaller>=6.0.0
opencv-python-headless
cryptography>=42.0.0
pillow>=10.0.0
